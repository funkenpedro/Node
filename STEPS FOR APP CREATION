Create a folder for project 
npm init
git init
add .gitignore file with node_modules
npm install joi
npm install express

add listener at end of file
const port = process.env.PORT || 3000;
app.listen(port, () => console.log(`listening on port ${port}`));
